Problems i faced while using Git


1) clone giving permission denied error
=>	cmd 1) ssh-keygen (to create a public key)

	cmd 2) cat ~/.ssh/id_rsa.pub (copy the output key)

	go to github -> settings -> SSH and GPG keys -> New SSH key -> paste and save.	
